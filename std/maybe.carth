(data (Maybe a)
  None
  (Some a))

(define unwrap!
  (fmatch (case (Some x) x)
          (case None (panic "unwrap! of None"))))

(define (map-maybe f)
  (fmatch (case (Some x) (Some (f x)))
          (case None None)))
