(define (main _)
  (seq (id (printInt (id 1337)))
       (seq (printInt 42)
            (printInt 69))))

(define: id
    (forall [a] (Fun a a))
  (let [[: ida
           (forall [a] (Fun a a))
           (fun x x)]]
    ida))

(define seq (: (fun (_ b) (: b b))
               (Fun  a b  b)))
