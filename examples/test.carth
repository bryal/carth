(type (List a)
  Empty
  (List a (List a)))

(define (main _)
  (seq (id (printInt (id 1337)))
       (seq (printInt 42)
            (printInt 69))))

(define: (id x)
    (forall [a] (Fun a a))
  x)

(define seq (: (fun (_ b) (: b b))
               (Fun  a b  b)))
