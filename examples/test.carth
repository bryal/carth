(define (main _)
  (seq (id (printInt (id 1337)))
       (seq (printInt 42)
            (printInt 69))))

(define id (: (fun x x) (Fun a a)))

(define seq (: (fun (_ b) (: b b))
               (Fun  a b  b)))
