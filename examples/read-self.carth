(import std)

(defun main [Unit]
  (let1 s (unwrap! (read_file "read-self.carth"))
    (apps seq
          (display "Printing ourselves...")
          (display "==================================================")
          (display s)
          (display "=================================================="))))
